import type { CwaConfig } from '@cwa/types'

export default {
  name: 'form-input-field',
  version: '1.0.0',
  customizable: {
    bgColor: { type: 'color', default: 'rgba(0,0,0,0.5)', cssVar: '--modal-bg-color' }
  },
  dependencies:[]
} as CwaConfig